<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>e-commerce</title>
    <link rel="stylesheet" href="/styles.css">
</head>
<body>
    <%- include('parts/nav.ejs') %>

    <h1>Update Product</h1>
    <form action="/update/<%= product.id %>" method="post">
        <div>
            <label for="title">Title :</label>
            <input id="title" type="text" name="title" required value="<%= product.title %>">    
        </div>
        <div>
            <label for="price">Price :</label>
            <input id="price" type="number" step="0.01" name="price" value="<%= product.price %>" required>
        </div>
        <div>
            <label for="description">Description :</label>
            <textarea id="description" name="description" required><%= product.description %></textarea>
        </div>
        <div>
            <label for="category">Category :</label>
            <input id="category" type="text" name="category" value="<%= product.category %>" required>
        </div>
        <div>
            <label for="image">Image URL :</label>
            <input id="image" type="text" name="image" value="<%= product.image %>">    
        </div>
        <div>
            <label for="rate">Rate :</label>
            <input id="rate" type="number" step="0.1" name="rate" value="<%= product.rating.rate %>" required>
        </div>
        <div>
            <label for="count">Count :</label>
            <input id="count" type="number" step="1" name="count" value="<%= product.rating.count %>" required>
        </div>
       <input type="submit" value="Update">
    </form>

    <%- include('parts/footer.ejs') %>
</body>
</html>